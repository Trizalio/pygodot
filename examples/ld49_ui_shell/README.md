# LD49 UI shell example

This example is a small LD49-style menu shell. It uses thin Control node
constructors, a copied texture asset, and file-backed GDScript for button
behavior.

It is not a full LD49 port. The goal is to prove that pygodot can declare a
container-heavy Godot 4 UI scene without adding a custom layout framework.
