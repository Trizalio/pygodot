# Mouse Input Example

Builds a tiny scene with a marker and click counter.

Left click moves the marker to the mouse position and increments the counter.

```powershell
$env:PYTHONPATH = "../../src"
python game.py
```
